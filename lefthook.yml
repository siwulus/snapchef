pre-commit:
  parallel: true
  jobs:
    - name: eslint
      glob: "*.{ts,tsx,astro}"
      run: npx eslint --fix {staged_files}
      stage_fixed: true

    - name: prettier
      glob: "*.{json,css,md}"
      run: npx prettier --write {staged_files}
      stage_fixed: true
